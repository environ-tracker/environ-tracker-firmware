description: |
  Silabs visible, IR and UV light sensor.

compatible: "silabs,si1132"

include: i2c-device.yaml

properties:
  nint-gpios:
    type: phandle-array
    required: false
    description: Active low interrupt pin to be used
